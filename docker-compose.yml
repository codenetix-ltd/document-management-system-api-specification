version: "3"
services:

  documentation:
    build:
      context: ./
      dockerfile: ./documentation/Dockerfile
    container_name: documentation
    volumes:
      - ./schema:/raml/src
    ports:
      - "80:80"
